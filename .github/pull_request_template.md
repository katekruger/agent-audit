## What changed and why

## Spec/code parity

- [ ] If this changes `py/` behavior, `spec/SPECIFICATION.md` is updated in
      the same PR (and vice versa) — see AGENTS.md.
- [ ] If this adds an attribute, I checked it doesn't restate an existing
      `gen_ai.*` or `llm.cost.*` attribute (`spec/mappings/otel-genai.md`).
- [ ] If this touches `examples/`, it still validates against the current
      JSON Schema (CI checks this, but faster to catch locally).

## Use of AI

Was this PR substantially drafted or generated by an AI tool? If so, note
it here — see CONTRIBUTING.md.
