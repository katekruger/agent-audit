# MCP server middleware

- Status: placeholder — targeted for v0.2 (`BUILD-PLAN.md` feature #11).

Target: a decorator/middleware that an arbitrary MCP server can apply to
emit `agent-audit` records automatically, without hand-instrumenting each
tool call. See [`spec/mappings/mcp.md`](../../spec/mappings/mcp.md) for the
MCP-specific field mapping this will implement against.

Not yet implemented.
