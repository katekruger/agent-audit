# Claude Code hooks integration

- Status: placeholder — see `BUILD-PLAN.md` feature #9 (v0.1) and
  [`spec/mappings/claude-code-hooks.md`](../../spec/mappings/claude-code-hooks.md)
  for the hook-to-phase mapping this will implement.

Target: a drop-in hook config plus a small HTTP receiver, so installing
`agent-audit` recording requires no code change to an existing Claude Code
setup — only hook configuration.

Not yet implemented.
